class Solution(object):
    def maxSubArray(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        sum = 0
        maximum = -sys.maxsize-1
        for num in nums:
            if sum< 0 : sum = 0
            sum += num
            maximum = max(sum, maximum)
        return maximum
            